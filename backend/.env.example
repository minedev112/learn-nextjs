# Copy to .env and adjust as needed.

# SQLAlchemy database URL (note the +psycopg driver).
DATABASE_URL=postgresql+psycopg://blog:blog@localhost:5432/blog

# Comma-separated allowed CORS origins, or "*" for all.
CORS_ORIGINS=*
